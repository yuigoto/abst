/**
 * type/Interceptor
 * ----------------------------------------------------------------------
 * Type signature for data interceptor methods.
 *
 * It receives data and spits the modified data.
 *
 * @since 0.1.0
 */
export type Interceptor = (data: any) => any;
